tags:
  - Team Management
summary: View teams management page
parameters:
  - name: id
    in: query
    type: integer
    required: true
    description: Game session ID
responses:
  200:
    description: Returns the teams management page
    content:
      text/html:
        schema:
          type: string
  302:
    description: Redirects to home if session not found
  404:
    description: Session not found
