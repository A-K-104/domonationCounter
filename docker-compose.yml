version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
      - sqlite_data:/app/instance
      - log_data:/app/logs
    restart: unless-stopped

volumes:
  sqlite_data:
  log_data:
